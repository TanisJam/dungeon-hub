# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# Logs
*.log
logs/

# IDE
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# OS
.DS_Store
Thumbs.db

# Supabase self-hosted: TODO el contenido es upstream (gestionado por bootstrap.sh),
# excepto los archivos que nosotros mantenemos (whitelisted abajo).
infra/supabase/*
infra/supabase/.[!.]*
!infra/supabase/bootstrap.sh
!infra/supabase/docker-compose.override.yml

# 5etools data — not versioned and NOT a submodule: populate it manually from
# the upstream mirror (see "Known setup gaps" in README.md).
data/5etools/

# Test coverage
coverage/

# Misc
*.swp
*.swo
.vercel
.env*

# Local-only heavy data / third-party inputs (not versioned)
data/
apps/web/public/world-maps/
docs/design_handoff_dungeon_hub/

# Skill registry cache (generated)
.atl/.skill-registry.cache.json

# CodeGraph index (generated per checkout, ~70MB — never share between checkouts)
.codegraph/
