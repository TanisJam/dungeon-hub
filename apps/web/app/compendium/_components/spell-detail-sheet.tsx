// SpellDetailSheet is superseded by the generic DetailSheet (ADR-4, REQ-CBROWSE-06).
// Kept as an empty re-export shim to avoid breaking any accidental import during the migration.
// TODO: Remove this file after Batch 1 is fully merged.

export {};
